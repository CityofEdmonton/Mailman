/***
 * I'm not too sure if I'll end up using this file much. It may be that I only need the 'Text is exactly' functionality.
 *
 */

Comparison = {
  'Text is exactly' : textIsExactly
};

function textIsExactly(text, mustBe) {
  return text === mustBe;
}
